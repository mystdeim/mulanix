<?php
$value = preg_match('/^$/', NULL, $val);
var_dump($value);
//if (is_array($var))
var_dump($val);
$arr = array('a', 'b', 'c');
foreach ($arr as $t) {
    var_dump($t);
    //continue;
    var_dump($t);
}